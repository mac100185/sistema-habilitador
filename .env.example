# ============================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# Sistema Habilitador - Variables de Configuración
# ============================================

# IMPORTANTE:
# - Copiar este archivo como .env en producción
# - NO commitear el archivo .env con credenciales reales
# - Actualizar los valores según el ambiente (desarrollo/producción)

# ============================================
# BASE DE DATOS PRINCIPAL (sisthabpro)
# ============================================
DB_HOST=db
DB_PORT=3306
DB_USER=quanium
DB_PASSWORD=quanium
DB_NAME=sisthabpro

# ============================================
# BASE DE DATOS API (seguridadapi)
# ============================================
DB_HOST_API=10.10.0.112
DB_PORT_API=3306
DB_USER_API=root
DB_PASSWORD_API=quanium
DB_NAME_API=seguridadapi

# ============================================
# CONFIGURACIÓN DEL SERVIDOR
# ============================================
PORT=7777
NODE_ENV=production

# ============================================
# CONFIGURACIÓN DE LA APLICACIÓN
# ============================================
# URL base del API (dejar vacío para detección automática)
API_HOST=

# Tiempo de espera para requests (en milisegundos)
REQUEST_TIMEOUT=30000

# Tiempo de espera para uploads (en milisegondos)
UPLOAD_TIMEOUT=120000

# ============================================
# CONFIGURACIÓN DE ARCHIVOS
# ============================================
# Tamaño máximo de archivo en bytes (10MB por defecto)
MAX_FILE_SIZE=10485760

# Tipos de archivo permitidos (separados por coma)
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/gif

# ============================================
# CONFIGURACIÓN DE SEGURIDAD
# ============================================
# Secret para JWT (generar uno único en producción)
JWT_SECRET=your-secret-key-here-change-in-production

# Longitud mínima de contraseña
MIN_PASSWORD_LENGTH=8

# ============================================
# CONFIGURACIÓN DE LOGS
# ============================================
LOG_LEVEL=info

# ============================================
# CONFIGURACIÓN DE MYSQL
# ============================================
MYSQL_ROOT_PASSWORD=quanium
MYSQL_DATABASE=sisthabpro
MYSQL_USER=quanium
MYSQL_PASSWORD=quanium

# ============================================
# NOTAS DE SEGURIDAD
# ============================================
# 1. Cambiar todas las contraseñas por defecto en producción
# 2. Usar contraseñas seguras (mínimo 16 caracteres, combinación de mayúsculas, minúsculas, números y símbolos)
# 3. Generar un JWT_SECRET único usando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. Configurar DB_HOST según el ambiente (usar 'db' para Docker, 'localhost' para desarrollo local)
# 5. Mantener este archivo .env fuera del control de versiones
